name: My Workflow

on:
  push: 
    branches: [master]

jobs:
  my-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
       
      - name: Test
        run: |
          echo "test"